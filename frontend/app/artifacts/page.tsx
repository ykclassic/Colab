export default function Artifacts(){return <><header className="page-head"><div><div className="eyebrow">Traceability</div><h1>Artifact explorer</h1><p>Versioned outputs are the durable hand-off between agents and workflow stages.</p></div></header><section className="card"><h2>Artifact lineage</h2><div className="timeline">{['Research','Strategy','Risk','Implementation','Validation'].map((s,i)=><div className="stage" key={s}><b>{i+1}</b><span>{s} artifact</span></div>)}</div></section><div className="notice section">Artifact records are currently exposed per workspace by the FastAPI backend. This screen establishes the visual contract; a full cross-workspace explorer should use the persistent artifact repository before claiming complete lineage coverage.</div></>}
